From fb391cdac87b74181d77ee90cfba26f664226947 Mon Sep 17 00:00:00 2001
From: akhilesh <akhileshpookkuttiyil@gmail.com>
Date: Wed, 23 Apr 2025 17:22:29 +0530
Subject: [PATCH 12/25] added footer section

---
 client/src/App.jsx               |  2 ++
 client/src/Components/Footer.jsx | 42 ++++++++++++++++++++++++++++++++
 2 files changed, 44 insertions(+)
 create mode 100644 client/src/Components/Footer.jsx

diff --git a/client/src/App.jsx b/client/src/App.jsx
index 4ac196a..5762439 100644
--- a/client/src/App.jsx
+++ b/client/src/App.jsx
@@ -3,6 +3,7 @@ import Navbar from "./Components/Navbar";
 import { Routes, Route, useLocation } from "react-router-dom";
 import Home from "./Pages/Home";
 import { Toaster } from "react-hot-toast";
+import Footer from "./Components/Footer";
 
 const App = () => {
   const isSellerPath = useLocation().pathname.includes("/seller");
@@ -20,6 +21,7 @@ const App = () => {
           <Route path="/" element={<Home />} />
         </Routes>
       </div>
+      {!isSellerPath && <Footer/> }
     </div>
   );
 };
diff --git a/client/src/Components/Footer.jsx b/client/src/Components/Footer.jsx
new file mode 100644
index 0000000..be0739e
--- /dev/null
+++ b/client/src/Components/Footer.jsx
@@ -0,0 +1,42 @@
+import React from "react";
+import { assets, footerLinks } from "../assets/assets";
+
+const Footer = () => {
+  return (
+    <div className="px-6 md:px-16 lg:px-24 xl:px-32 mt-24 bg-primary/10">
+      <div className="flex flex-col md:flex-row items-start justify-between gap-10 py-10 border-b border-gray-500/30 text-gray-500">
+        <div>
+          <img className="w-34 md:w-32" src={assets.logo} alt="logo" />
+          <p className="max-w-[410px] mt-6">
+            Bringing fresh groceries to your doorstep. Convenient shopping,
+            quality products, and swift delivery to make life easier—every day,
+            every time.
+          </p>
+        </div>
+        <div className="flex flex-wrap justify-between w-full md:w-[45%] gap-5">
+          {footerLinks.map((section, index) => (
+            <div key={index}>
+              <h3 className="font-semibold text-base text-gray-900 md:mb-5 mb-2">
+                {section.title}
+              </h3>
+              <ul className="text-sm space-y-1">
+                {section.links.map((link, i) => (
+                  <li key={i}>
+                    <a href={link.url} className="hover:underline transition">
+                      {link.text}
+                    </a>
+                  </li>
+                ))}
+              </ul>
+            </div>
+          ))}
+        </div>
+      </div>
+      <p className="py-4 text-center text-sm md:text-base text-gray-500/80">
+        Copyright {new Date().getFullYear()} © InstaBasket.com All Right Reserved.
+      </p>
+    </div>
+  );
+};
+
+export default Footer;
-- 
2.46.1.windows.1

